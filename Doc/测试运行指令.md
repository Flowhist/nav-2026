> 进入/home/embotic/nav_workspace路径
>
> 左侧雷达ip 192.168.1.111
>
> 右侧雷达ip 192.168.1.112

# 移动

### step 1. 启动底盘 CAN、STM32 手柄通信与控制路由（建图/导航都需要）

```bash
source install/setup.bash

./src/finav/base_drive.sh
```



# 建图

### step 1. 启动slam-toolbox建图服务

```bash
source install/setup.bash

export FINAV_REPO_DIR=$PWD/src/finav

export FINAV_MAPS_DIR=$PWD/src/finav/maps

ros2 launch finav map.launch.py
```

### step 2. 启动rviz查看效果

> 远程桌面：sudo ./start_vnc.sh
>

```bash
source install/setup.bash

rviz2 -d src/finav/rviz/mapping.rviz
```

### step 3. 保存地图

```bash
./src/finav/scripts/tool/save_map.sh
```

### step 4. 使用本地地图编辑器

从源码工作区启动：

```bash
python3 src/finav/scripts/map_location/map_editor.py
```

构建并 `source install/setup.bash` 后，也可以运行：

```bash
ros2 run finav map_editor.py
```

服务只监听 `127.0.0.1`，默认会打开浏览器。窗口内可直接编辑目标点、可旋转矩形禁行区和巡航路线；每次完整操作都会自动保存到地图目录中的 `<地图名>.editor.yaml`。

常用启动参数：

```bash
# 指定地图根目录和初始地图，不自动打开浏览器
python3 src/finav/scripts/map_location/map_editor.py \
  --maps-dir src/finav/maps --map longcorridor --no-browser
```

“导出新地图”会复制目标点和路线，并把禁行区写入新的地图栅格。源地图不会被修改。

地点统一在 Web 页面维护，不再提供本地手动注册脚本。

# 导航

### step 1. 启动slam-toolbox导航服务

```bash
source install/setup.bash

export FINAV_REPO_DIR=$PWD/src/finav

export FINAV_MAPS_DIR=$PWD/src/finav/maps

ros2 launch finav nav.launch.py
```

### step 2. 启动rviz查看效果

> 远程桌面：sudo ./start_vnc.sh

```bash
source install/setup.bash

rviz2 -d src/finav/rviz/navigation.rviz
```

### step 3. 手动重定位

导航链路启动后，另开一个终端执行一次：

```bash
source install/setup.bash

ros2 run finav nav_relocate.py
```

也可以指定地图：

```bash
source install/setup.bash

ros2 run finav nav_relocate.py --ros-args -p map_file:=longcorridor
```
